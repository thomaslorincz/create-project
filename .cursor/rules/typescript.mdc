<!--
  Generated by @thomaslorincz/ai-rules.
  Run: bunx @thomaslorincz/ai-rules sync
-->
---
description: TypeScript coding standards and type conventions
alwaysApply: false
globs: **/*.ts, **/*.tsx
---

# TypeScript

- Prefer interfaces over types unless an interface is not possible
- Avoid using ReturnType over defining standalone types for return values
- For string union enums, define a const array with `as const` and derive the type from it - this gives both a type (for args) and an iterable list of all values (for validation, UI, etc.):

  ```ts
  export const ALL_MODEL_TASK_TYPES = ['segmentation', 'classification'] as const;
  export type ModelTaskType = (typeof ALL_MODEL_TASK_TYPES)[number];
  ```

  Name the const array `ALL_<NAME>` (e.g. `ALL_MODEL_TASK_TYPES`). Prefer this over inline string union types.
